const domUpdates = {
  currentUser: null,
  todayDate: null,

  defineValues(currentUser, todayDate) {
    this.currentUser = currentUser;
    this.todayDate = todayDate;
  }


}
export default domUpdates;